<!-- generated: component composition analysis -->
**Page:** `/en/home/careers/overview`  
**Template cluster:** `11790138db` (careers landing page)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Careers landing page

Full-width landing template for Careers > Overview, alternating light/grey/dark bands that introduce working at the company and funnel to jobs, programs and employee stories.

1. `side-by-side` — grey hero band: large heading and paragraph on the left with a `button-group`-style pair of pill CTAs ('Search jobs' primary red, 'Join talent community' outline), and a YouTube video embed on the right. Margin below: 100px (content-buffer, 10rem; 5rem on mobile).
2. `richtext` — centered section heading 'We're everywhere you look…' with supporting copy and a lead-in line. Margin below: 50px (section-buffer, 5rem).
3. `video-block` — row of three captioned YouTube embeds (employee spotlight videos). Margin below: 100px (content-buffer, 10rem; 5rem on mobile).
4. `side-by-side` — feature row: photo left, 'Early career opportunities' heading, text and black 'Find a program' button right. Margin below: 100px (content-buffer, 10rem; 5rem on mobile).
5. `side-by-side` — feature row: photo left, 'Employee volunteerism' heading, text and black 'Learn more' button right. Margin below: ~90px (visual estimate).
6. `categories-block` — full-width dark band 'Join an award-winning workplace': intro sentence, central circular trophy icon, and six award title/source mentions arranged three per side (42px mg-tp-m between items). Margin below: 100px (content-buffer, 10rem; 5rem on mobile).
7. `richtext` — centered heading 'Life at Avery Dennison' with one-line intro. Margin below: 50px (section-buffer, 5rem).
8. `carousel-block` — 4-card grid of employee stories (photo, 'EMPLOYEE' eyebrow, name/role title, 'Read more >' link). Margin below: ~40px (visual estimate).
9. `button` — centered black 'View more' pill linking to the Life at AD listing. Margin below: 100px (content-buffer, 10rem; 5rem on mobile).

Vertical rhythm: major bands are separated by `content-buffer` (10rem = 100px desktop, 50px mobile; 1rem = 10px on this site); headings sit 50px (`section-buffer`) above their content; items inside the awards band use `mg-tp-m` (42px).
