<!-- generated: component composition analysis -->
**Page:** [/en/home/news/company-blog/adf-scholars-spotlight-august-2024](https://www.averydennison.com/en/home/news/company-blog/adf-scholars-spotlight-august-2024.html)  
**Template cluster:** `4ed3086843` (company blog article (callout + side-by-side profiles))  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Company blog article (callout + side-by-side profiles)

Richest blog-article variant: the body mixes plain richtext with two-column arrangements — a grey callout/info box and several image-beside-text profile rows.

1. `back-link` — '← Back to all blogs'. Margin below: ~64px.
2. `article-header` — title, category label, date. Margin below: ~40px.
3. `image` — lead photo. Margin below: ~40px.
4. `social-share` — share icons + divider. Margin below: 45px (mg-bt-s).
5. `richtext` — intro paragraph. Margin below: ~40px.
6. `side-by-side` — program-history text on the left, grey callout box (red top border, heading + bulleted country list) on the right. Margin below: ~40px.
7. `richtext` — bridge paragraphs + first profile sub-heading. Margin below: 50px (section-buffer).
8. `side-by-side` — portrait photo (with caption) beside profile text; image position alternates left/right across instances. Margin below: ~40px.
9. `richtext` — quotes and continuation. Margin below: ~40px.
10. `side-by-side` — second photo/text row. Margin below: ~40px.
11. `richtext` — continuation + second profile sub-heading. Margin below: 50px (section-buffer).
12. `side-by-side` — captioned portrait beside text. Margin below: ~40px.
13. `richtext` — closing paragraphs. Margin below: ~48px to divider.
14. `back-link` — bottom back link. Margin below: 100px (content-buffer).

Sidebar: `carousel-block` ('You may also like', 3 posts) then `form-block` ('Sign up for updates').

**Vertical rhythm:** `section-buffer` (5rem / 50px) between major chapters; ~25px inside chapters; `content-buffer` (10rem / 100px) at page end.
