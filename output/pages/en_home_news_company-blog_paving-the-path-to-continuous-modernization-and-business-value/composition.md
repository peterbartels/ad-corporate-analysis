<!-- generated: component composition analysis -->
**Page:** `/en/home/news/company-blog/paving-the-path-to-continuous-modernization-and-business-value`  
**Template cluster:** `bc75f8061e` (leadership perspectives Q&A article)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

> **Template note:** this layout is shared by 2 pages:
> - `/en/home/news/company-blog/paving-the-path-to-continuous-modernization-and-business-value`
> - `/en/home/news/leadership-perspectives/nick-colisto/paving-the-path-to-continuous-modernization-and-business-value`

# Leadership perspectives Q&A article

Executive interview template in the blog layout: main column article with author card and grey pull-quote panels, plus the standard right sidebar (related articles + newsletter signup).

1. `back-link` — top return link ('Back to leadership perspectives'). ~40px below.
2. `article-header` — H1 title and publish date (no standfirst; the byline is a separate author card). ~32px below.
3. `article-author` — author card: round headshot, name, job title and a 'LinkedIn Profile' icon button. ~40px below.
4. `image` — hero image (executive portrait on a branded background). ~40px below.
5. `social-share` — Share icons (Facebook, X, LinkedIn, email). ~64px below.
6. `richtext` — intro paragraphs and the first block of Q&A (bold question headings, answer paragraphs). 50px below (section-buffer, 5rem).
7. `article-quote` — grey full-width panel with a large quotation mark and pull-quote text. 50px below (section-buffer).
8. `richtext` — second Q&A block. 50px below (section-buffer).
9. `article-quote` — second grey pull-quote panel. 50px below (section-buffer).
10. `richtext` — closing Q&A block, followed by a horizontal rule. ~64px below.
11. `carousel-block` — sidebar 'You may also like' with three related article cards. ~48px below in the sidebar.
12. `form-block` — sidebar 'Sign up for updates' email form. Ends the sidebar.
13. `back-link` — bottom return link. 100px (content-buffer, 10rem; halved on mobile) to page end.

Vertical rhythm: body sections are separated by authored `section-buffer` spacers (5rem = 50px); the page opens after a `content-buffer` top offset and closes with a 10rem `content-buffer` (5rem on mobile).
