<!-- generated: component composition analysis -->
**Page:** `/en/home/industries/optica-supply-chain-solutions`  
**Template cluster:** `15556534c3` (industry landing page (featured-solution cards variant))  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Industry landing page (featured-solution cards variant)

Variant of the Industries template that replaces the product-category link sections with two large featured-solution cards, each combining heading, copy, CTA button and photo.

1. `richtext` — Centered H1 product-line title (with ™). The page opens with a 100px top spacer (content-buffer). Margin below: 50px (section-buffer).
2. `richtext` — Centered intro paragraph with an inline link, plus a bold centered tagline. Margin below: 100px (content-buffer).
3. `side-by-side` — Featured card 1: white elevated card containing H2, description copy, black "Learn more" pill button (left) and product photo (right). Margin below: 50px (section-buffer).
4. `side-by-side` — Featured card 2, same card layout with different solution content. Margin below: 100px (content-buffer), then a full-width black horizontal rule (spacer line--black).
5. `richtext` — H2 "Industry solutions from our businesses". Margin below: 50px (section-buffer).
6. `image-link-block` — Shared grid of 15 white box-button cards (business name + circular arrow icon) linking to Avery Dennison brand sites. Margin below: 100px (content-buffer) to page end.

## Vertical rhythm

Same buffer scale as the other industry pages: `section-buffer` = 5rem (80px), `content-buffer` = 10rem (160px), with a `line--black` hairline spacer before the shared business grid.
