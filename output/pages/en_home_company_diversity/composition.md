<!-- generated: component composition analysis -->
**Page:** `/en/home/company/diversity`  
**Template cluster:** `037bdbc85b` (diversity culture page)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Diversity culture page

Editorial storytelling page alternating text/photo rows, a brand video, a dark awards band and a closing illustrated section.

1. `richtext` — centered H1 "Diversity". Margin below: 50px (section-buffer, 5rem).
2. `side-by-side` — "Every voice. Every day." heading and paragraphs left, employee photo right. Margin below: 100px (content-buffer, 10rem).
3. `side-by-side` — photo left, large statement text right. Margin below: 100px (content-buffer).
4. `side-by-side` — large statement text left, paired photos right. Margin below: 100px (content-buffer).
5. `video-block` — responsive YouTube embed (animated brand video). Margin below: 50px (section-buffer).
6. `richtext` — two text columns ("Global awareness and recognition" / "Regional DEI councils"). Margin below: 100px (content-buffer).
7. `richtext` — dark band heading "Living our values: Our award-winning teams" + intro. Margin below: 50px (section-buffer).
8. `categories-block` — trophy badge with a 3-column grid of ~8 award name + source entries on the dark band. Margin below: 100px (content-buffer).
9. `richtext` — light band "Employee resource groups" heading, paragraphs and ERG category list. Margin below: ~48px (visual estimate).
10. `image` — full-width diverse-crowd illustration closing the page. Margin below: ~0px (flush to page end).

## Vertical rhythm
content-buffer 10rem/100px between story rows and bands; section-buffer 5rem/50px inside bands between heading and content.
