<!-- generated: component composition analysis -->
**Page:** `/en/home/news/company-blog/next-level-innovation-z2010-passes-the-ice-bucket-test`  
**Template cluster:** `88318d8550` (company blog article (legacy skeleton, YouTube video))  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Company blog article — legacy skeleton with YouTube video (Z2010 passes the ice bucket test)

Two-column blog article on the older skeleton (no explicit spacers; ends with mg-bt-l), structurally identical to the hosted-video variant but embedding YouTube instead of JW Player.

1. `back-link` — '← Back to all blogs' text link. Margin below: ~64px (visual estimate).
2. `article-header` — H1 headline, bold standfirst/deck sentence, author byline (name, role, business/region) and display date. Margin below: ~40px (visual estimate).
3. `image` — full-column hero photo. Margin below: ~40px (visual estimate).
4. `social-share` — 'Share' + Facebook / X / LinkedIn / email icons above a divider rule. Margin below: ~48px (visual estimate).
5. `richtext` — intro body paragraphs. Margin below: ~40px (visual estimate).
6. `video-block` — YouTube embed (youtube-player parbase, iframe https://www.youtube.com/embed/UIYRklFrCGg, 16:9). Margin below: ~40px (visual estimate).
7. `richtext` — closing body paragraphs. Margin below: ~64px (visual estimate) above a divider rule.
8. `back-link` — repeated '← Back to all blogs' link. Margin below: ~80px (mg-bt-l spacer) to page end.
9. `carousel-block` — sidebar 'You may also like': 3 stacked related-blog cards. Margin below: ~48px (visual estimate).
10. `form-block` — sidebar 'Sign up for updates' email signup (avd-form POST /bin/services/contacts). Margin below: page-end buffer.

## Vertical rhythm

Legacy article spacing (~40-64px component margins); mg-bt-l (~80px) closes the page. Sidebar column beside the body throughout.
