<!-- generated: component composition analysis -->
**Page:** `/en/home/careers/talent-community`  
**Template cluster:** `f50859ebae` (talent community landing page)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Talent community landing page

Two-column page: a grid of career-field link boxes in the main column, CTA sidebar on the right.

1. `richtext` — H1 "Talent community" and an intro paragraph explaining the program. Margin below: 50px (section-buffer, 5rem).
2. `image-link-block` — grid (3 columns) of ~22 small horizontal box-buttons, each a text-only card with a career field name and arrow icon linking to the talent-community signup for that field. Margin below: 100px (content-buffer, 10rem).
3. `richtext` — sidebar lead "Find your next career opportunity". Margin below: ~24px (visual estimate).
4. `button` — "Search jobs". Margin below: 50px (section-buffer, 5rem).
5. `richtext` — sidebar heading "Interested in a specific role?" with copy. Margin below: ~24px (visual estimate).
6. `button` — "Sign up for job alerts". Margin below: 100px (content-buffer) to page end.

## Vertical rhythm
content-buffer 10rem/100px frames the page and follows the box-button grid; section-buffer 5rem/50px separates intro from grid and the sidebar CTA groups.
