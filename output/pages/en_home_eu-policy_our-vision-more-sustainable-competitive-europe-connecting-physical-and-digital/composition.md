<!-- generated: component composition analysis -->
**Page:** `/en/home/eu-policy/our-vision-more-sustainable-competitive-europe-connecting-physical-and-digital`  
**Template cluster:** `ad01f6e366` (campaign article page with highlight lists)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Campaign article page with highlight lists

An EU-policy vision article whose signature elements are stacked orange "policy ask" highlight rows between rich text sections.

1. `short-hero` — full-width image-only banner (25rem tall). Margin below: ~48px (content-buffer class).
2. `on-page-navigation` — campaign subpage links plus a second-row link. Margin below: ~56px.
3. `richtext` — centered H1 and intro paragraph. Margin below: ~64px.
4. `richtext` — first thematic H2 with "Enabling conditions" bullet list and a "How can we make it happen?" lead-in. Margin below: ~24px.
5. `html-embed` — orange highlight list: seven stacked full-width orange rows, each one policy action sentence (each row is its own raw HTML block). Margin below: ~64px.
6. `richtext` — second thematic H2 with bullets and lead-in. Margin below: ~24px.
7. `html-embed` — second orange highlight list of five rows. Margin below: ~64px.
8. `side-by-side` — short closing statement beside a photo. Margin below: ~80px.
9. `cta-block` — gray band with "Our Policy Priorities for the 2024–2029 Term" and a red "Read more" button. Margin below: ~80px.
10. `richtext` — centered "Contact Our Experts" heading. Margin below: ~40px.
11. `form-block` — Name / Email / Inquiry form with reCAPTCHA and "Send message". Margin below: ~96px.
12. `categories-block` — four teaser cards to sibling campaign pages on a gray band. Margin below: ~64px to page end.

## Vertical rhythm
Band-based spacing (~60–80px) like the other campaign pages; the orange highlight rows stack flush with no internal gaps. Only the hero carries a named buffer class.
