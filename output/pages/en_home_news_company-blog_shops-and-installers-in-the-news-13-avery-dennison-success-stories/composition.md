<!-- generated: component composition analysis -->
**Page:** [/en/home/news/company-blog/shops-and-installers-in-the-news-13-avery-dennison-success-stories](https://www.averydennison.com/en/home/news/company-blog/shops-and-installers-in-the-news-13-avery-dennison-success-stories.html)  
**Template cluster:** `913ef61857` (blog article with boxed card sections)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Blog article with boxed card sections

Curated 'in the news' roundup article: intro copy followed by three elevated white card sections, each grouping a themed set of linked bullet items around a photo; standard right sidebar.

1. `back-link` — top 'Back to all blogs' link. ~40px below.
2. `article-header` — H1 title, author/team line, publish date. ~32px below.
3. `image` — hero image. ~40px below.
4. `social-share` — Share icons. ~64px below.
5. `richtext` — intro copy. 50px below (section-buffer, 5rem).
6. `cta-block` — boxed card 'Shop stories': heading, bulleted list of external press links, a photo mid-card, more bullets. 50px below (section-buffer).
7. `cta-block` — boxed card 'Full color coverage', same pattern. 50px below (section-buffer).
8. `cta-block` — boxed card 'Noteworthy initiatives', same pattern. 50px below (section-buffer).
9. `richtext` — closing CTA copy and italic disclaimer, then a horizontal rule. ~64px below.
10. `carousel-block` — sidebar 'You may also like', three related article cards. ~48px below in sidebar.
11. `form-block` — sidebar newsletter signup. Ends the sidebar.
12. `back-link` — bottom return link. 100px (content-buffer) to page end.

Vertical rhythm: `section-buffer` spacers (5rem = 50px) between the intro, each card and the closing copy; page closes with a 10rem `content-buffer` (5rem mobile). The cards carry their own internal padding (~40px) and drop shadow.
