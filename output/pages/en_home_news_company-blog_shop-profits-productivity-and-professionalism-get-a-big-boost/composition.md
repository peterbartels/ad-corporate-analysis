<!-- generated: component composition analysis -->
**Page:** [/en/home/news/company-blog/shop-profits-productivity-and-professionalism-get-a-big-boost](https://www.averydennison.com/en/home/news/company-blog/shop-profits-productivity-and-professionalism-get-a-big-boost.html)  
**Template cluster:** `1e53c15767` (blog article with infographic and inline videos)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Blog article with infographic and inline videos

Rich blog article mixing copy, photos, a full-width statistics infographic image and three embedded testimonial videos; standard right sidebar.

1. `back-link` — top 'Back to all blogs' link. ~40px below.
2. `article-header` — H1 title, author/team line, publish date. ~32px below.
3. `image` — hero image. ~40px below.
4. `social-share` — Share icons. ~64px below.
5. `richtext` — long opening copy. 50px below (section-buffer, 5rem).
6. `image` — inline photo. ~40px below.
7. `richtext` — body copy. ~40px below.
8. `image` — full-width statistics infographic (authored as one image, not a live stat block). 50px below (section-buffer).
9. `richtext` — body copy with quotes. ~40px below.
10. `video-block` — first embedded testimonial video (YouTube in a JW Player wrapper). ~40px below.
11. `richtext` — link/caption copy. ~40px below.
12. `video-block` — second testimonial video. ~40px below.
13. `richtext` — copy. ~40px below.
14. `video-block` — third testimonial video. ~40px below.
15. `richtext` — closing class-descriptions section, then a horizontal rule. ~64px below.
16. `carousel-block` — sidebar 'You may also like', three related article cards. ~48px below in sidebar.
17. `form-block` — sidebar newsletter signup. Ends the sidebar.
18. `back-link` — bottom return link. 100px (content-buffer) to page end.

Vertical rhythm: `section-buffer` spacers (5rem = 50px) between major sections, ~40px intrinsic margins inside the text/media flow, `content-buffer` (10rem, 5rem mobile) closing the page.
