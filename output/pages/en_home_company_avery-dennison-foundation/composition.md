<!-- generated: component composition analysis -->
**Page:** [/en/home/company/avery-dennison-foundation](https://www.averydennison.com/en/home/company/avery-dennison-foundation.html)  
**Template cluster:** `04314f2a12` (foundation overview landing page)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Foundation overview landing page

Long-form corporate landing page mixing centered editorial sections, a dark stat band, testimonial quote panels and an article card list.

1. `richtext` — centered two-line H1. Margin below: ~24px (visual estimate).
2. `on-page-navigation` — 5 pipe-separated anchor links to the page sections. Margin below: 50px (section-buffer, 5rem).
3. `side-by-side` — intro paragraphs on the left, embedded YouTube video on the right. Margin below: 100px (content-buffer, 10rem).
4. `statistics-block` — full-width dark band: eyebrow "Our 2025 impact", display figure "$6.1+ million", award badge image, summary paragraph and "View CSR Report" button. Margin below: 100px (content-buffer).
5. `article-quote` — gray quote panel with circular portrait, quote, name and role. Margin below: 100px (content-buffer).
6. `richtext` — centered "Grantmaking" heading + mission text. Margin below: 50px (section-buffer).
7. `categories-block` — three icon columns (impact areas: title + description). Margin below: 50px (section-buffer).
8. `richtext` — application invitation text. Margin below: ~32px (visual estimate).
9. `button` — "Learn more". Margin below: 50px (section-buffer).
10. `article-quote` — grantee testimonial (no portrait). Margin below: 100px (content-buffer).
11. `richtext` — centered "Employee-driven programs" heading + text with inline links. Margin below: 50px (section-buffer).
12. `image` — program photo. Margin below: ~32px (visual estimate).
13. `richtext` — program name and description. Margin below: 50px (section-buffer).
14. `article-quote` — employee testimonial. Margin below: 100px (content-buffer).
15. `richtext` — centered "Disaster relief" heading + text. Margin below: 100px (content-buffer).
16. `richtext` — centered "Foundation stories" heading + intro. Margin below: 50px (section-buffer).
17. `carousel-block` — 4 story cards (image, date, title, Read more) with a "View more" button. Margin below: 100px (content-buffer) to page end.

## Vertical rhythm
Major sections separated by content-buffer 10rem/100px; headings sit 5rem/50px (section-buffer) above their section content. Mobile halves content-buffer to 5rem.
