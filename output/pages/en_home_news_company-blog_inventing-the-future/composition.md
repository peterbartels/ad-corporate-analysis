<!-- generated: component composition analysis -->
**Page:** `/en/home/news/company-blog/inventing-the-future`  
**Template cluster:** `2947ff1173` (company blog article (legacy skeleton, hosted video))  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Company blog article — legacy skeleton with hosted video (Inventing the Future)

Two-column blog article on the older skeleton (no explicit spacers; ends with mg-bt-l). Long-form Foundation story with a bold standfirst, a self-hosted JW Player video mid-article, and an extended richtext body.

1. `back-link` — '← Back to all blogs' text link. Margin below: ~64px (visual estimate).
2. `article-header` — H1 headline, bold standfirst/deck sentence, author byline (name, role, organization) and display date. Margin below: ~40px (visual estimate).
3. `image` — full-column branded hero visual (Avery Dennison Foundation banner). Margin below: ~40px (visual estimate).
4. `social-share` — 'Share' + Facebook / X / LinkedIn / email icons above a divider rule. Margin below: ~48px (visual estimate).
5. `richtext` — short intro paragraphs with an inline program link. Margin below: ~40px (visual estimate).
6. `video-block` — self-hosted video ('10 Years of InvEnt in India') in a JW Player embed (video-player parbase, poster frame + play button, 16:9). Margin below: ~40px (visual estimate).
7. `richtext` — long article body: ~15 paragraphs of program history and reflections with inline links. Margin below: ~64px (visual estimate) above a divider rule.
8. `back-link` — repeated '← Back to all blogs' link. Margin below: ~80px (mg-bt-l spacer) to page end.
9. `carousel-block` — sidebar 'You may also like': 3 stacked related-blog cards. Margin below: ~48px (visual estimate).
10. `form-block` — sidebar 'Sign up for updates' email signup (avd-form POST /bin/services/contacts). Margin below: page-end buffer.

## Vertical rhythm

Legacy article spacing (~40-64px component margins, no section-buffers); mg-bt-l (~80px) closes the page. Sidebar column beside the body throughout.
