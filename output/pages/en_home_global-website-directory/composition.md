<!-- generated: component composition analysis -->
**Page:** `/en/home/global-website-directory`  
**Template cluster:** `9f8919c612` (website directory page)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Website directory page

Left-aligned A-Z directory of all Avery Dennison web properties: a title followed by ~30 repeated brand sections, each listing the available language/region editions as links.

1. `richtext` — Centered H1 "Global website directory". The page opens with a 100px top spacer (content-buffer). Margin below: ~25px.
2. `table` — ~30 repeated directory sections. Each section is a bold site/brand name heading followed by an alternating-rows table of underlined language links (e.g. English | Deutsch | 简体中文). Large brands (Graphics Solutions, Label and Packaging Materials) subdivide their rows by region (North America, Europe, Asia Pacific, Latin America). Sections are separated by a 1px gray rule (spacer line--gray) plus 50px (section-buffer). Margin below the last section: 100px (content-buffer) to page end.

## Vertical rhythm

Uniform repetition: `section-buffer` (5rem/50px) plus a `line--gray` hairline between every directory entry; `content-buffer` (10rem/100px) at the top and bottom of the page.
