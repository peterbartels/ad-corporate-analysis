<!-- generated: component composition analysis -->
**Page:** `/en/home/eu-policy/europes-waste-problem`  
**Template cluster:** `9586d18532` (campaign article page with stat bands)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Campaign article page with stat bands

The richest EU-policy article template: rich text and side-by-sides punctuated by full-width red statistic bands, a download CTA, a video section, a form and the sibling-page card band.

1. `short-hero` — full-width image-only banner (25rem tall). Margin below: ~48px (content-buffer class).
2. `on-page-navigation` — campaign subpage links, current page bold. Margin below: ~48px.
3. `richtext` — centered H1 and intro. Margin below: ~56px.
4. `richtext` — first section heading and cited paragraphs. Margin below: ~24px.
5. `side-by-side` — small stat callout card with photo (textimage-horizontal). Margin below: ~64px.
6. `statistics-block` — full-width band of two large red line-art icons with big numbers and captions (HTML embeds). Flows straight into the next band.
7. `cta-block` — dark band promoting a downloadable report with a "Read more" button. Margin below: ~80px.
8. `richtext` — "From linear to circular" text with an inline icon-and-percentage stat callout. Margin below: ~40px.
9. `side-by-side` — white-paper promo (quote box, text, cover image, download link). Margin below: ~48px.
10. `statistics-block` — second red stat band (two consumer percentages with icons and source). Margin below: ~64px.
11. `richtext` — DPP section heading and paragraphs with adjacent photo. Margin below: ~24px.
12. `button` — dark pill ("More on DPP"). Margin below: ~72px.
13. `richtext` — gray-band section heading ("How DPP unlocks sustainability"). Margin below: ~32px.
14. `video-block` — embedded YouTube interview video with a caption line. Margin below: ~48px.
15. `statistics-block` — three icon columns with one-line captions. Margin below: ~80px.
16. `richtext` — centered "Contact Our Experts" heading. Margin below: ~40px.
17. `form-block` — Name / Email / Inquiry form with reCAPTCHA and "Send message". Margin below: ~96px.
18. `categories-block` — four teaser cards to sibling campaign pages on a gray band. Margin below: ~64px to page end.

## Vertical rhythm
Alternating white, gray, dark and red full-bleed bands supply the rhythm (~40–80px gaps); the only named spacer is the hero's content-buffer class. Stat bands are bespoke HTML embeds rather than standard AEM components.
