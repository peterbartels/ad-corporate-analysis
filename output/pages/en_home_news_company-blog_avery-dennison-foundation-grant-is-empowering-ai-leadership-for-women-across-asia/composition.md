<!-- generated: component composition analysis -->
**Page:** `/en/home/news/company-blog/avery-dennison-foundation-grant-is-empowering-ai-leadership-for-women-across-asia`  
**Template cluster:** `5733708c94` (company blog article)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Company blog article

Compact blog article: captioned lead photo, one inline photo, otherwise pure richtext body.

1. `back-link` — '← Back to all blogs'. Margin below: ~64px.
2. `article-header` — title, category label, date. Margin below: ~40px.
3. `image` — lead photo with caption. Margin below: ~40px.
4. `social-share` — share icons + divider. Margin below: 45px (mg-bt-s).
5. `richtext` — first body section with inline links and quoted statements. Margin below: 50px (section-buffer).
6. `image` — inline photo with caption. Margin below: ~40px.
7. `richtext` — remaining body paragraphs. Margin below: ~48px to divider.
8. `back-link` — bottom back link. Margin below: 100px (content-buffer).

Sidebar: `carousel-block` ('You may also like', 3 posts) then `form-block` ('Sign up for updates').

**Vertical rhythm:** `section-buffer` (5rem / 50px) after the opening body, ~25px around the inline image, `content-buffer` (10rem / 100px) at page end.
