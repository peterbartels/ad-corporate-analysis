<!-- generated: component composition analysis -->
**Page:** [/en/home/news/company-blog/top-10-supreme-wrapping-film-colors-2025](https://www.averydennison.com/en/home/news/company-blog/top-10-supreme-wrapping-film-colors-2025.html)  
**Template cluster:** `4c4eadfdc5` (blog countdown/list article)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Blog countdown/list article

Top-N listicle: intro copy, then eleven repetitions of a full-width photo followed by a numbered richtext entry (countdown 10 to 1 plus a lead-in image), closing CTA copy; standard right sidebar.

1. `back-link` — top 'Back to all blogs' link. ~40px below.
2. `article-header` — H1 title, author/team line, publish date. ~32px below.
3. `image` — hero image (title overlay baked into the asset). ~40px below.
4. `social-share` — Share icons. ~64px below.
5. `richtext` — intro copy. 50px below (section-buffer, 5rem).
6. `image` + `richtext` — countdown item: full-width vehicle photo, then a bold numbered heading ('10. Color name - SKU') with a descriptive paragraph. This image/text pair repeats eleven times down to item 1, each element followed by an 50px section-buffer.
7. `richtext` — closing copy with request-a-swatch/CTA links, then a horizontal rule. ~64px below.
8. `carousel-block` — sidebar 'You may also like', three related article cards. ~48px below in sidebar.
9. `form-block` — sidebar newsletter signup. Ends the sidebar.
10. `back-link` — bottom return link. 100px (content-buffer) to page end.

Vertical rhythm: strict `section-buffer` cadence (5rem = 50px) between every image and text block in the countdown; the page closes with a 10rem `content-buffer` (5rem mobile).
