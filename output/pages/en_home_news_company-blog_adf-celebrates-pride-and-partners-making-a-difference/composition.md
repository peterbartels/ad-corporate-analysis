<!-- generated: component composition analysis -->
**Page:** [/en/home/news/company-blog/adf-celebrates-pride-and-partners-making-a-difference](https://www.averydennison.com/en/home/news/company-blog/adf-celebrates-pride-and-partners-making-a-difference.html)  
**Template cluster:** `9541292103` (company blog article (multi-image))  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Company blog article (multi-image)

Same two-column blog article template as the standard variant, distinguished by a long body broken into several sub-headed chapters, each closed by a captioned photo.

1. `back-link` — '← Back to all blogs'. Margin below: ~64px.
2. `article-header` — title, category label, date. Margin below: ~40px.
3. `image` — lead photo. Margin below: ~40px.
4. `social-share` — share icon row + divider. Margin below: 45px (mg-bt-s).
5. `richtext` — intro plus first chapter with a bold sub-heading. Margin below: 50px (section-buffer, 5rem).
6. `image` — captioned inline photo. Margin below: ~40px.
7. `richtext` — second chapter (sub-heading, quotes). Margin below: ~40px.
8. `image` — captioned inline photo. Margin below: ~40px.
9. `richtext` — third chapter. Margin below: ~40px.
10. `image` — captioned inline photo. Margin below: ~40px.
11. `richtext` — closing paragraphs. Margin below: ~48px to divider.
12. `back-link` — bottom back link. Margin below: 100px (content-buffer, 10rem).

Sidebar: `carousel-block` ('You may also like', 3 related posts) then `form-block` ('Sign up for updates' email capture).

**Vertical rhythm:** major breaks use `section-buffer` (5rem / 50px); image/caption-to-text transitions ~25px; page-end gap `content-buffer` (10rem / 100px).
