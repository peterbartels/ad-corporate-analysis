<!-- generated: component composition analysis -->
**Page:** `/en/home/company/values`  
**Template cluster:** `9c9a7658bc` (values overview page)  
*Rem base: the site sets `html { font-size: 62.5% }` at desktop widths (>=1180px), so 1rem = 10px in all margin values below.*

# Values overview page

A short brand page: a centered intro followed by a staggered grid of eight value cards.

1. `richtext` — centered H1 "Eight values. One team." with a two-line intro paragraph about company culture. Margin below: 100px (content-buffer, 10rem).
2. `statistics-block` — eight info cards in a two-column staggered grid. Each card has a value heading, a short description, and a line-art SVG icon on the right; alternating cards sit on a light gray box background, and one card carries an inline "Learn more" link. Margin below: 100px (content-buffer, 10rem; 5rem mobile) to page end.

## Vertical rhythm
Only content-buffer 10rem (160px desktop, 5rem mobile) is used, above and below the card grid; spacing inside the grid comes from the checkerboard card layout itself.
