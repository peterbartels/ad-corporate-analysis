{
  "name": "ad-component-inventory",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "sync": "python3 scripts/images.py && node scripts/sync.mjs",
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "^4.123.0"
  }
}
